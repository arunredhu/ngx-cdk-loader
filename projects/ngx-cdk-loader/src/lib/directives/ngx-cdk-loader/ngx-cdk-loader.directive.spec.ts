import { NgxCdkLoaderDirective } from './ngx-cdk-loader.directive';

describe('NgxCdkLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxCdkLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
