/**
 * Loader configuration model
 */
export interface NgxCdkLoaderConfig {
  /** Loader timeout in miliseconds. This is used to hide the loader after a specific timeout, if loader is still visible */
  loadingTimeout: number;
}
